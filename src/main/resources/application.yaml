spring:
  application:
    name: home-auth-service
server:
  port: 8081
  servlet:
    context-path: /home/auth

oauth:
  google:
    client:
      id: prod configured in Kubernetes ConfigMap. local configured as VM argument

application:
  id: home-stack-auth
  security:
    jwt:
      validity: 1800

